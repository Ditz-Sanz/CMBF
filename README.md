# CMBF
$ pkg update && upgrade

$ pkg install python

$ pkg install git

$ pip install bs4

$ pip install requests

$ pip install mechanize

$ pip install futures

$ pkg install git

$ rm -rf CMBF

$ git clone https://www.github.com/Ditz-Sanz/CMBF

$ cd CMBF

$ python CMBF.py
